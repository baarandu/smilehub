name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm audit --audit-level=high || true
      - run: npm run build
      - run: npm run lint
      - run: npx tsc --noEmit
      - run: npm test -- --run

  mobile:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: mobile
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: mobile/package-lock.json
      - run: npm ci
      - run: npm audit --audit-level=high || true
      # Dívida de tipagem no app mobile (tabelas ausentes em types/database.ts,
      # ~86 erros pré-existentes). Não-bloqueante até a tipagem ser regenerada
      # a partir do banco; remover o continue-on-error ao zerar os erros.
      - run: npx tsc --noEmit
        continue-on-error: true
